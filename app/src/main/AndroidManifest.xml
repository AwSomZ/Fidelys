<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fidelyss">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fidelyss">
        <activity android:name=".TransacationPopup" android:theme="@style/AppTheme.CustomTheme"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MouvementActivity"  android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".Redirection" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity3" />
        <activity android:name=".RegisterActivity2" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>